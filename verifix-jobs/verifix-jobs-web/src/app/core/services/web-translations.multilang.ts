import { TranslationCatalog } from './web-translations.util';

// Previously contained kk, tg, ky supplementary translations.
// Only uz_lat and ru are supported now.
export const WEB_MULTILANG_TRANSLATIONS: TranslationCatalog = {};
