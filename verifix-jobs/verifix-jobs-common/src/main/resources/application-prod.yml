spring:
  jpa:
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        generate_statistics: false
  liquibase:
    enabled: true

  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false

  # === DATABASE POOL — DAU 10K (~1000 concurrent) ===
  datasource:
    hikari:
      maximum-pool-size: ${HIKARI_MAX_POOL_SIZE:50}
      minimum-idle: ${HIKARI_MIN_IDLE:10}
      connection-timeout: 10000
      idle-timeout: 300000
      max-lifetime: 600000
      leak-detection-threshold: 30000

  # === REDIS POOL ===
  data:
    redis:
      timeout: 5000
      lettuce:
        pool:
          max-active: ${REDIS_POOL_MAX_ACTIVE:40}
          max-idle: ${REDIS_POOL_MAX_IDLE:20}
          min-idle: ${REDIS_POOL_MIN_IDLE:10}

  # === ASYNC & SCHEDULING ===
  task:
    execution:
      pool:
        core-size: ${ASYNC_CORE_POOL:15}
        max-size: ${ASYNC_MAX_POOL:100}
        queue-capacity: ${ASYNC_QUEUE_CAPACITY:500}
    scheduling:
      pool:
        size: ${SCHEDULER_POOL_SIZE:10}

# === TOMCAT — DAU 10K ===
server:
  compression:
    enabled: true
    mime-types: application/json,text/html,text/plain,text/css,application/javascript
    min-response-size: 1024
  error:
    include-stacktrace: never
    include-message: never
  tomcat:
    max-threads: ${TOMCAT_MAX_THREADS:400}
    min-spare-threads: ${TOMCAT_MIN_SPARE:40}
    accept-count: ${TOMCAT_ACCEPT_COUNT:200}
    max-connections: ${TOMCAT_MAX_CONNECTIONS:2000}
    connection-timeout: 10000

# === Swagger disabled ===
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

logging:
  level:
    root: WARN
    uz.verifix.jobs: INFO
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
    org.telegram.telegrambots: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      show-details: when-authorized
  health:
    elasticsearch:
      enabled: false

app:
  kafka:
    enabled: ${KAFKA_ENABLED:true}
  elasticsearch:
    enabled: ${ELASTICSEARCH_ENABLED:true}
  minio:
    enabled: ${MINIO_ENABLED:false}
  jwt:
    secret: ${JWT_SECRET}
  cors:
    allowed-origins: ${CORS_ORIGINS:https://job.verifix.uz}
